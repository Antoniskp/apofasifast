# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=voting_db
POSTGRES_USER=voting_user
POSTGRES_PASSWORD=voting_password

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Keycloak
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin123
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=voting
KEYCLOAK_CLIENT_ID=api
# IMPORTANT: Replace with a secure random string in production (e.g., generated with: openssl rand -base64 32)
KEYCLOAK_CLIENT_SECRET=your-api-client-secret-here

# Frontend Keycloak
NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8080
NEXT_PUBLIC_KEYCLOAK_REALM=voting
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=frontend

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=localhost
MINIO_PORT=9000

# API
API_PORT=3001
API_URL=http://localhost:3001

# Frontend
FRONTEND_PORT=3000
